{% block comment_form %}
<div class="db-panel db-card">
  <form action="{{ path('create_comment', { article_id: article.id}) }}" method="post">
    <p>
      <textarea name="body" class="db-input"></textarea>
    </p>
    <p>
      <button type="submit" class="db-button">Post Comment</button>
    </p>
  </form>
</div>
{% endblock %}